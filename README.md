river-container-examples
========================

This is a Maven multi-module project that contains an example service (the typical "hello world") that
can be deployed to the Apache River Container.
